spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/muse
    username: postgres
    password: Prasanth
  flyway:
    enabled: true
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: qASV5/5N2p5bV8c+m8g/VwGvXzZtCg4bSj6mR/lIuN3xVwEaBfD+c2aFj7HkE9pWqYvXzZtCg4b
  access-ttl-ms: 900000        # 15 minutes
  refresh-ttl-ms: 604800000    # 7 days

app:
  frontend-origin: http://localhost:5173
#  postgres
prekey:
  claim:
    ttl: 300     # seconds before reclaim
  reclaim:
    interval:
      ms: 60000  # run every 60 seconds

      # Websocket allowed origins for dev
      messaging:
        ws:
          allowed-origins: "*"

logging:
  level:
    org:
      springframework:
        security: DEBUG
        security.oauth2: DEBUG